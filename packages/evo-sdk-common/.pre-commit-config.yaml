repos:
- repo: https://github.com/charliermarsh/ruff-pre-commit
  rev: v0.14.0 # keep in sync with pyproject.toml
  hooks:
    - id: ruff
      args: [ --fix, --no-cache]
    - id: ruff-format
      args: [--no-cache]